// the value stored in noop is being imported from the noop directory
import noop from './noop';
// the value stored in toArray is being imported from the to-array directory
import toArray from './to-array';
// the value stored in createElement is being imported from the create-element directory
import createElement from './create-element';

// the values stored in 3 variables are exported
export {
  noop,
  toArray,
  createElement
};
